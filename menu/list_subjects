 list_subjects(){

for file_to_list in ../sgms_data/subjects/*.sub; do
	[ -e "$file_to_list" ] || continue
	
	code=$(sed -n '1p' "$file_to_list")
	name=$(sed -n '2p' "$file_to_list")
	credits=$(sed -n '3p' "$file_to_list")

	echo "Code: $code| Name: $name |Credits:
$credits"
done
}






