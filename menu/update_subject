 update_subject(){
 

file="../sgms_data/subjects"
if [[ -z "$(ls -A "$file")" ]]; then
echo "no data found"
return
fi



ls ../sgms_data/subjects
  echo "enter a subject code" 
  read code
  
 file_update="../sgms_data/subjects/$code.sub"
  
   if ! [ -f "$file_update" ]; then
        echo "file not founded"
         return
   
        
    fi   
   while true; do  
  echo "Enter a new name" 
  read name
    if [[ ! $name =~ ^[A-Za-z[:space:]]{2,10}$ ]]; then
        echo "Invalid name!"
       else
       break
    fi
    done
    
    while true; do 
  echo "Enter a new credit" 
  read credit
  
   if ! [[ $credit =~ ^[1-6]$ ]]; then
        echo "Invalid Credit! between[1-6]"
      else
      break
    fi
  done
  
  echo "$code">"$file_update"
  echo "$name">>"$file_update"
  echo "$credit">>"$file_update"
  
    
  echo "update sucssessfully"






























}
