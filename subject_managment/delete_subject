


function delete_subject(){
   echo "Enter s subject to be deleted" name
   
   if ! [[ $name =~ ?^[A-Za-z ]]; then
   	echo "not valid subject name" 
   else
   	return
   fi
     grade_file="grade_managment/grades/$name.grd"
     sub_file=Subject_managment/ Subjects/$name
   if [[ -f sub_file ]]; then 
   	return
   else
   	echo "no such subject added"
   fi
 while true ; do  
   echo "you really want to delete $name ? (Y/N)" read cho

  if ! [[ $cho == Y||N || y||n ]]; then
  
   echo "not valid choise" 
 
 else
   rm sub_file
   rm -f "$grade_file"
   
   echo "function deleted sucssefully "
    break;;
    fi
    done
    
   
   
   
}
