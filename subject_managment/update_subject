


function update_subject(){

  echo "enter a subject code" read code
  
  file_update = "Subjects/$code.sub"
  
   if ! [ -f "$file_update" ];
        echo "file not founded"
         return
   
        
    fi   
     
  echo "Enter a new name" read name
  echo "Enter a new credit" read credit
  
  echo "$code">>"$file"
  echo "$name">>"$file"
  echo "$credit">>"$file"
  
    if ! [[ $credit =~ ^[1-6]$ ]]; then
        echo "Invalid Credit!"
        return
    fi
  
  echo "update sucssessfully"






























}
